// Simple config file for the frontend. CI will overwrite this file at deploy time
// with the production API origin. Leave blank for local development where the
// static site and API are same-origin.
window.GGG_API_ORIGIN = window.GGG_API_ORIGIN || '';
