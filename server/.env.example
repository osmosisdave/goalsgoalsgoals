# Copy this file to .env and fill in the values (DO NOT COMMIT .env)
# A strong random value for signing JWTs
JWT_SECRET=change_this_to_a_strong_random_value

# NOTE: admin users are created explicitly using the `create_admin.js` utility.
# Do NOT store initial admin passwords in this file for long-term use.

# MongoDB Atlas connection string (SRV format). Replace with your cluster's URI.
# Example: mongodb+srv://ggg_admin:MyP%40ssw0rd@cluster0.abcd3.mongodb.net/goalsdb?retryWrites=true&w=majority
MONGODB_URI=

# Optional: port to run server
PORT=4000

# Allowed origin for CORS (set to your frontend origin)
# Example: https://your-username.github.io
ALLOWED_ORIGIN=https://goalsgoalsgoals.onrender.com
